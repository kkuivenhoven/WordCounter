<h1>Static #home</h1>


<%= form_for :static, :url => { :controller => 'static', :action => 'submit_page' } do |f| %>

	<%= f.label :input_text %> <br>
	<%= f.text_area :input_text, cols: 50, rows: 20 %><br>

	<%= f.submit 'Submit' %>

<% end %>
